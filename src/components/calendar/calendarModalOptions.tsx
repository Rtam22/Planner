import Button from "../common/button";

function CalendarModalOptions() {
  return (
    <div>
      <Button className="">Create task</Button>
      <Button className="">View all tasks</Button>
    </div>
  );
}

export default CalendarModalOptions;
